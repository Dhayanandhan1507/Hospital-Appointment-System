import { ArrowUpRight } from 'lucide-react'
export function PageTitle({title,text,action}){return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h1 className="text-2xl sm:text-3xl">{title}</h1><p className="mt-1 text-sm text-slate-500">{text}</p></div>{action}</div>}
export function StatCard({icon:Icon,label,value,trend,color='brand'}){const colors={brand:'bg-brand-50 text-brand-700 dark:bg-brand-900/30',blue:'bg-blue-50 text-blue-600 dark:bg-blue-900/30',emerald:'bg-emerald-50 text-emerald-600 dark:bg-emerald-900/30',amber:'bg-amber-50 text-amber-600 dark:bg-amber-900/30'};return <div className="card"><div className="flex items-start justify-between"><span className={`grid size-12 place-items-center rounded-xl ${colors[color]}`}><Icon/></span>{trend&&<span className="flex items-center text-xs font-semibold text-emerald-500"><ArrowUpRight size={14}/>{trend}</span>}</div><b className="mt-5 block text-3xl text-navy dark:text-white">{value}</b><span className="text-sm text-slate-500">{label}</span></div>}
export function Table({heads,children}){return <div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase tracking-wider text-slate-500 dark:bg-slate-800"><tr>{heads.map(h=><th key={h} className="px-5 py-3">{h}</th>)}</tr></thead><tbody className="divide-y">{children}</tbody></table></div>}
